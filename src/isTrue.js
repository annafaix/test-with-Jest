import React from 'react';

// Skriv testfall för isTrue(x). Funktionen ska returnera true om parametern är true och false i alla andra fall.

function isTrue(x){
  if(x === "salted caramel"){
    return true;
  }else
    return false;
};

export {isTrue};
