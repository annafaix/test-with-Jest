import {deleteAllUsers, addUser, getUser} from '../TDD1.js';

test('mock deleteAllUsers function', ()=>{
  let deleteAllUsers = jest.fn();
  // console.log(deleteAllUsers);
  // // expect(mockFunc).toBeCalled();
  // expect(deleteAllUsers.mock.calls.length).toBeGreaterThan(0);
})
