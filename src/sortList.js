import React from 'react';

// Skriv testfall för funktionen sortList(list), som tar en lista som parameter och returnerar en ny lista som sorterar elementen som strängar i bokstavsordning.

function sortList(list){

  return list.sort();
}

export {sortList};
